import { useState } from "react";
import RestaurantContainer from "./RestaurantContainer";
const Body = () => {
  return (
    <div className="body">
      <RestaurantContainer />
    </div>
  );
};

export default Body;
